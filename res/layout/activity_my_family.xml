<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rtl_share_main"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white"
    android:orientation="vertical" >

    <include
        android:id="@+id/common_nav_only_back"
        layout="@layout/common_nav_only_back" />

    <TextView
        android:id="@+id/tv_one_key_dredge_rmg_flow_share"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minHeight="30dp"
        android:textColor="@color/gray_font"
        android:textSize="@dimen/text_size_14" />

    <ListView
        android:id="@+id/listView_my_family_dhqlw"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/tv_one_key_dredge_rmg_flow_share"
        android:layout_below="@id/common_nav_only_back"
        android:background="@android:color/transparent"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@android:color/transparent"
        android:fadingEdge="none"
        android:fastScrollEnabled="false"
        android:footerDividersEnabled="true"
        android:headerDividersEnabled="true"
        android:dividerHeight="@dimen/dp_8dp"
        android:listSelector="@android:color/transparent"
        android:smoothScrollbar="true"
        tools:listitem="@layout/item_my_family" />

</RelativeLayout>